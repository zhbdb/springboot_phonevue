spring:
  datasource:
    username: root
    password: root
    url: jdbc:mysql://localhost:3306/phone_store_demo?useUnicode=true&characterEncoding=utf-8&serverTimezone=UTC
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    #打印sql
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        #格式化format_sql sql
server:
  port: 8081

  #spring:
    #  datasource:
    #    url: jdbc:mysql://localhost:3306/library?useUnicode=true&characterEncoding=UTF-8&serverTimezone=Asia/Shanghai
    #    username: root
    #    password: root
    #    driver-class-name: com.mysql.cj.jdbc.Driver
    #  jpa:
    #    show-sql: true
    #    properties:
    #      hibernate:
    #        format_sql: true
  #server:
  #  port: 8181
